*---------------------------------------------------------------------*
*    view related FORM routines
*---------------------------------------------------------------------*
*...processing: /SPROLIMS/VCCATG................................*
FORM GET_DATA_/SPROLIMS/VCCATG.
  PERFORM VIM_FILL_WHERETAB.
*.read data from database.............................................*
  REFRESH TOTAL.
  CLEAR   TOTAL.
  SELECT * FROM /SPROLIMS/TCCATG WHERE
(VIM_WHERETAB) .
    CLEAR /SPROLIMS/VCCATG .
/SPROLIMS/VCCATG-MANDT =
/SPROLIMS/TCCATG-MANDT .
/SPROLIMS/VCCATG-OTKAT =
/SPROLIMS/TCCATG-OTKAT .
/SPROLIMS/VCCATG-DESCR_OTKAT =
/SPROLIMS/TCCATG-DESCR_OTKAT .
<VIM_TOTAL_STRUC> = /SPROLIMS/VCCATG.
    APPEND TOTAL.
  ENDSELECT.
  SORT TOTAL BY <VIM_XTOTAL_KEY>.
  <STATUS>-ALR_SORTED = 'R'.
*.check dynamic selectoptions (not in DDIC)...........................*
  IF X_HEADER-SELECTION NE SPACE.
    PERFORM CHECK_DYNAMIC_SELECT_OPTIONS.
  ELSEIF X_HEADER-DELMDTFLAG NE SPACE.
    PERFORM BUILD_MAINKEY_TAB.
  ENDIF.
  REFRESH EXTRACT.
ENDFORM.
*---------------------------------------------------------------------*
FORM DB_UPD_/SPROLIMS/VCCATG .
*.process data base updates/inserts/deletes.........................*
LOOP AT TOTAL.
  CHECK <ACTION> NE ORIGINAL.
MOVE <VIM_TOTAL_STRUC> TO /SPROLIMS/VCCATG.
  IF <ACTION> = UPDATE_GELOESCHT.
    <ACTION> = GELOESCHT.
  ENDIF.
  CASE <ACTION>.
   WHEN NEUER_GELOESCHT.
IF STATUS_/SPROLIMS/VCCATG-ST_DELETE EQ GELOESCHT.
     READ TABLE EXTRACT WITH KEY <VIM_XTOTAL_KEY>.
     IF SY-SUBRC EQ 0.
       DELETE EXTRACT INDEX SY-TABIX.
     ENDIF.
    ENDIF.
    DELETE TOTAL.
    IF X_HEADER-DELMDTFLAG NE SPACE.
      PERFORM DELETE_FROM_MAINKEY_TAB.
    ENDIF.
   WHEN GELOESCHT.
  SELECT SINGLE FOR UPDATE * FROM /SPROLIMS/TCCATG WHERE
  OTKAT = /SPROLIMS/VCCATG-OTKAT .
    IF SY-SUBRC = 0.
    DELETE /SPROLIMS/TCCATG .
    ENDIF.
    IF STATUS-DELETE EQ GELOESCHT.
      READ TABLE EXTRACT WITH KEY <VIM_XTOTAL_KEY> BINARY SEARCH.
      DELETE EXTRACT INDEX SY-TABIX.
    ENDIF.
    DELETE TOTAL.
    IF X_HEADER-DELMDTFLAG NE SPACE.
      PERFORM DELETE_FROM_MAINKEY_TAB.
    ENDIF.
   WHEN OTHERS.
  SELECT SINGLE FOR UPDATE * FROM /SPROLIMS/TCCATG WHERE
  OTKAT = /SPROLIMS/VCCATG-OTKAT .
    IF SY-SUBRC <> 0.   "insert preprocessing: init WA
      CLEAR /SPROLIMS/TCCATG.
    ENDIF.
/SPROLIMS/TCCATG-MANDT =
/SPROLIMS/VCCATG-MANDT .
/SPROLIMS/TCCATG-OTKAT =
/SPROLIMS/VCCATG-OTKAT .
/SPROLIMS/TCCATG-DESCR_OTKAT =
/SPROLIMS/VCCATG-DESCR_OTKAT .
    IF SY-SUBRC = 0.
    UPDATE /SPROLIMS/TCCATG ##WARN_OK.
    ELSE.
    INSERT /SPROLIMS/TCCATG .
    ENDIF.
    READ TABLE EXTRACT WITH KEY <VIM_XTOTAL_KEY>.
    IF SY-SUBRC EQ 0.
      <XACT> = ORIGINAL.
      MODIFY EXTRACT INDEX SY-TABIX.
    ENDIF.
    <ACTION> = ORIGINAL.
    MODIFY TOTAL.
  ENDCASE.
ENDLOOP.
CLEAR: STATUS_/SPROLIMS/VCCATG-UPD_FLAG,
STATUS_/SPROLIMS/VCCATG-UPD_CHECKD.
MESSAGE S018(SV).
ENDFORM.
*---------------------------------------------------------------------*
FORM READ_SINGLE_/SPROLIMS/VCCATG.
  SELECT SINGLE * FROM /SPROLIMS/TCCATG WHERE
OTKAT = /SPROLIMS/VCCATG-OTKAT .
/SPROLIMS/VCCATG-MANDT =
/SPROLIMS/TCCATG-MANDT .
/SPROLIMS/VCCATG-OTKAT =
/SPROLIMS/TCCATG-OTKAT .
/SPROLIMS/VCCATG-DESCR_OTKAT =
/SPROLIMS/TCCATG-DESCR_OTKAT .
ENDFORM.
*---------------------------------------------------------------------*
FORM CORR_MAINT_/SPROLIMS/VCCATG USING VALUE(CM_ACTION) RC.
  DATA: RETCODE LIKE SY-SUBRC, COUNT TYPE I, TRSP_KEYLEN TYPE SYFLENG.
  FIELD-SYMBOLS: <TAB_KEY_X> TYPE X.
  CLEAR RC.
MOVE /SPROLIMS/VCCATG-OTKAT TO
/SPROLIMS/TCCATG-OTKAT .
MOVE /SPROLIMS/VCCATG-MANDT TO
/SPROLIMS/TCCATG-MANDT .
  CORR_KEYTAB             =  E071K.
  CORR_KEYTAB-OBJNAME     = '/SPROLIMS/TCCATG'.
  IF NOT <vim_corr_keyx> IS ASSIGNED.
    ASSIGN CORR_KEYTAB-TABKEY TO <vim_corr_keyx> CASTING.
  ENDIF.
  ASSIGN /SPROLIMS/TCCATG TO <TAB_KEY_X> CASTING.
  PERFORM VIM_GET_TRSPKEYLEN
    USING '/SPROLIMS/TCCATG'
    CHANGING TRSP_KEYLEN.
  <VIM_CORR_KEYX>(TRSP_KEYLEN) = <TAB_KEY_X>(TRSP_KEYLEN).
  PERFORM UPDATE_CORR_KEYTAB USING CM_ACTION RETCODE.
  ADD: RETCODE TO RC, 1 TO COUNT.
  IF RC LT COUNT AND CM_ACTION NE PRUEFEN.
    CLEAR RC.
  ENDIF.

ENDFORM.
*---------------------------------------------------------------------*
